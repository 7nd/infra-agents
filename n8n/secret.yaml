# Ключ ровно N8N_ENCRYPTION_KEY — так требует existingEncryptionKeySecret
# community-charts/n8n чарта.
apiVersion: v1
kind: Secret
metadata:
  name: n8n-encryption-key
  namespace: agents
stringData:
  N8N_ENCRYPTION_KEY: ${n8n_encryption_key}
